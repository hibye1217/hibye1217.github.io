function run(){
    const inputTag = document.getElementById("input");
    const outputTag = document.getElementById("output");

    const str = inputTag.value().split();
    inputTag.innerHTML = ""; outputTag.innerHTML = "";
    if (str == ""){}
}